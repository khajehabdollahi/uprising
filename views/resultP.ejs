 <% layout('layout/boilerplate') %> 



<h2>Here is the result:</h2>

 <ul>
    <% for(let sch of school) { %>
      <div class="card">
        <div class="row">
          <div class="col-md-12">

           
            
 <a href="/news/<%= sch.id %>/<%=currentUser.id %> "><%= sch.schoolsname %></a>

          </div>
        </div>
      </div>
   

   <%} %>
  </ul>